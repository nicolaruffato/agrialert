<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Agrialert" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="bottomNavSelection">@color/white</item>
    </style>

    <style name="Theme.Agrialert" parent="Base.Theme.Agrialert" />
</resources>